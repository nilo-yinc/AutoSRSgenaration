# 🚀 DocuVerse Testing Guide

## Backend API Testing
1. Start the backend server: `npm run dev` in `backend/`
2. Use Postman or curl to test endpoints in `routes/`

## Python Service Testing
1. Activate virtual environment in `backend/`
2. Run `uvicorn backend.beta.main:app`
3. Verify at `http://localhost:8000`

## Frontend Testing
1. Run `npm run dev` in `frontend/`
2. Verify UI at `http://localhost:5173`
